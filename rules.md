# AI Creative Library - エージェントルール

## 基本ルール
- 日本語でコメントやコミットメッセージを書くこと
- コードの変更前に必ず計画を提示して承認を求めること
- 存在しないパッケージやAPIを勝手に使わないこと
- エラーが出たら自分で調べて修正を試みること

## デザインルール
- ダークモード + ネオングラデーション + パーティクルエフェクト等の「AIっぽい」デザインは禁止
- 背景色は白系（#FAF7F2）をベースにすること
- フォントはGoogle Fontsから取得（日本語: Noto Sans JP）
- 画像のプレースホルダーには暖色系のグラデーション背景を使う（ネオン色は禁止）
- レスポンシブ対応は必須（スマホ幅320px〜対応）

## コーディングルール
- TypeScript推奨
- コンポーネントは小さく分割する
- 画像・動画のデータはsrc/data/にJSONまたはTS配列で管理
- ハードコーディングを避け、データドリブンで表示する
- CSSはTailwind CSSを使用

## 画像生成ルール
- NanoBanaProで画像を生成する場合、プロンプトは日本語で具体的に指示する
- 生成画像はpublic/images/gallery/に保存
- ファイル名は英数字ケバブケースで（例: salon-spring-campaign.png）

## やってはいけないこと
- ユーザーの承認なしにファイルを削除しないこと
- 外部APIキーを要求するサービスを勝手に組み込まないこと
- node_modulesやビルド成果物をGitにコミットしないこと
- 本番環境に未テストのコードをデプロイしないこと
- **既存のGitHubリポジトリには絶対にアクセス・変更・削除しないこと**
- **git remote add / git push 等のリモート操作はユーザーの明示的な承認なしに実行しないこと**
- **リポジトリの作成・削除・名前変更をエージェントが勝手に行わないこと**
- **現在のプロジェクトフォルダ以外のディレクトリにアクセスしないこと**

## GitHub関連ルール（厳守）
- ローカルでのgit init、git add、git commitまではエージェントが実行してよい
- git remote add、git push、git clone等のリモート操作は必ずユーザーに確認してから実行
- 操作していいリポジトリは https://github.com/koji99911/ai-creative-library.git のみ
- koji99911の他のリポジトリには絶対にアクセスしないこと
- pushする前に必ず「pushしていいですか？」とユーザーに確認すること
